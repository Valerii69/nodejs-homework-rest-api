const isValidId = require("./isValidId");
const validateBody = require("./validateBody");
const authenticate = require("./authenticates");

module.exports = {
  isValidId,
  validateBody,
  authenticate,
};
